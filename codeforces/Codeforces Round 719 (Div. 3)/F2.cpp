#include<bits/stdc++.h>
using namespace std;
int n,t,ans,k;
vector<bool> solve;
void solve(int l,int r,int can0){
    if(r-l+1==can0)
}
int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    cin >> n >> t;
    cout << "? 1 " << n << '\n';
    cout.flush();
    cin >> ans;
    solve(1,n,ans);
    while(t--){
        
    }
}