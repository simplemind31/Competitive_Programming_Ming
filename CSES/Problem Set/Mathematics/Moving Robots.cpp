#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef long double ld;
int k;
int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    cin >> k;
    /*
    expected empty cells=sum(number of empty cells if the configuration is g) for each possible g where g can repeat/ total number of configurations
    = sum of for each cell i,j, the number of ways 
    */
}