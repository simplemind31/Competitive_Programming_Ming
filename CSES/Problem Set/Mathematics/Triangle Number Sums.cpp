#include <bits/stdc++.h>
#define MOD 1000000007
using namespace std;
typedef long long ll;
ll t,n;
int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    cin >> t;
    while(t--){
        cin >> n;
        // binary search, se permite 0?

        /*
        x*(x+1)/2
        */
    }
}