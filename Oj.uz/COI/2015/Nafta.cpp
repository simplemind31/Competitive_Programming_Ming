#include <bits/stdc++.h>
using namespace std;
int n,m;
char mat[2000][2000];
int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);cout.tie(0);
    cin >> n >> m;
    for(int i=0;i<n;i++){
        for(int j=0;j<m;j++){
            cin >> mat[i][j];
        }
    }
    vector<pair<pair<int,int>,int>> rango
    for(int i=0;i<n;i++){
        for(int j=0;j<m;j++){
            if(mat[i][j]=='.')continue;

        }
    }
}